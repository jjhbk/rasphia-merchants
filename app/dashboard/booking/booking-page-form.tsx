"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
type Service = { id: string; name: string; description: string | null; duration: number };
export function BookingPageForm({ services, publicUrl }: { services: Service[]; publicUrl: string }) { const router = useRouter(); const [notice, setNotice] = useState(""); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; setSaving(true); setError(""); const data = new FormData(form); const response = await fetch("/api/workspace/services", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id: data.get("id") || undefined, name: data.get("name"), description: data.get("description"), duration: Number(data.get("duration")) }) }); const result = await response.json().catch(() => null); setSaving(false); if (!response.ok) { setError(result?.error || "Could not save service."); return; } form.reset(); setNotice("Booking service saved."); router.refresh(); } async function remove(id: string) { await fetch(`/api/workspace/services?id=${id}`, { method: "DELETE" }); router.refresh(); } return <><section className="payment-offerings settings-card"><p className="section-label">Bookable services</p><h2>Services on your booking page.</h2>{services.map((service) => <div className="settings-service" key={service.id}><div><b>{service.name}</b><p>{service.description || "No description"} · {service.duration} minutes</p></div><button className="text-button" type="button" onClick={() => remove(service.id)}>Remove</button></div>)}<form className="payment-connect-form" onSubmit={submit}><input type="hidden" name="id" /><div className="field-row"><label>Service name<input name="name" required placeholder="Consultation" /></label><label>Duration<input name="duration" type="number" min="5" max="480" defaultValue="30" required /></label></div><label>Description <span>(optional)</span><textarea name="description" rows={2} /></label><button className="button button-outline" disabled={saving}>{saving ? "Saving…" : "Add service"}</button></form>{notice && <p className="form-success">{notice}</p>}{error && <p className="form-error">{error}</p>}</section><section className="public-url-card"><p className="section-label">Your public booking page</p><h2>Share this link with customers.</h2><a href={publicUrl} target="_blank" rel="noreferrer">{publicUrl} ↗</a></section></>; }
